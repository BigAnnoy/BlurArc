# 测试报告：video_metadata 缓存修复（P1-2）

**测试日期**：2026-06-29  
**测试脚本**：`test_p1_2_video_metadata.py`  
**测试对象**：`backend/api_server.py` 中 `_cached_video_metadata` 函数（L706-722）  
**测试结果**：✅ **全部通过（8/8）**

---

## 一、测试环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows |
| Python 版本 | 3.x (Anaconda) |
| 缓存实现 | `functools.lru_cache(maxsize=500)` |
| 缓存 key | `(file_path_str, file_mtime)` |
| 依赖模块 | `backend.video_processor.VideoProcessor.extract_metadata` |

---

## 二、测试用例与结果

### 测试 1：首次请求（缓存未命中）

| 项目 | 内容 |
|------|------|
| 目的 | 验证首次请求时缓存未命中，正确调用底层提取函数 |
| 方法 | 调用 `_cached_video_metadata(path, mtime)`，检查 `cache_info()` |
| 预期 | `misses=1, hits=0`，`extract_metadata` 被调用 1 次 |
| 实际 | `misses=1, hits=0`，调用 1 次 |
| 结果 | ✅ 通过 |

---

### 测试 2：重复请求（缓存命中）

| 项目 | 内容 |
|------|------|
| 目的 | 验证相同参数重复请求时缓存命中，不再调用底层函数 |
| 方法 | 相同 `(path, mtime)` 连续调用 3 次，检查调用次数和缓存统计 |
| 预期 | `extract_metadata` 只调用 1 次，`hits=2` |
| 实际 | `extract_metadata` 调用 1 次，`hits=2, misses=1`，三次返回结果完全一致 |
| 结果 | ✅ 通过 |

---

### 测试 3：文件修改后缓存失效

| 项目 | 内容 |
|------|------|
| 目的 | 验证文件 mtime 变化后缓存正确失效 |
| 方法 | 第一次用 `mtime_1` 请求，第二次用 `mtime_2`（模拟文件修改），检查返回数据 |
| 预期 | 两次返回不同的元数据，`misses=2` |
| 实际 | 第一次返回 `duration=100s, 1920x1080`，修改后返回 `duration=200s, 3840x2160`，`misses=2` |
| 结果 | ✅ 通过 |

---

### 测试 4：不同视频的缓存隔离

| 项目 | 内容 |
|------|------|
| 目的 | 验证不同文件路径的缓存互不干扰 |
| 方法 | 请求视频 A、视频 B、再请求视频 A，检查各自返回值 |
| 预期 | A 和 B 返回不同数据，第三次请求命中 A 的缓存 |
| 实际 | 视频 A `duration=30s`，视频 B `duration=90s`，再次请求 A 返回 `duration=30s`（缓存命中），`misses=2, hits=1` |
| 结果 | ✅ 通过 |

---

### 测试 5：性能对比（缓存命中 vs 未命中）

| 项目 | 内容 |
|------|------|
| 目的 | 量化缓存命中带来的性能提升 |
| 方法 | Mock `extract_metadata` 为 10ms 延迟函数，分别测量缓存未命中和命中的耗时 |
| 预期 | 缓存命中耗时 < 10ms，性能提升 > 90% |

| 指标 | 数值 |
|------|------|
| 缓存未命中耗时 | 11.030 ms |
| 缓存命中耗时 | 0.003 ms |
| 性能提升倍数 | **4412x** |
| 耗时减少比例 | **100.0%**（远超 90% 目标） |

| 结果 | ✅ 通过 |
|------|------|

---

### 测试 6：缓存大小限制（maxsize=500）

| 项目 | 内容 |
|------|------|
| 目的 | 验证 LRU 缓存大小限制和淘汰策略 |
| 方法 | 连续请求 600 个不同视频（超过 maxsize=500），检查缓存大小和 LRU 淘汰 |
| 预期 | `currsize <= 500`，最早插入的条目被淘汰 |
| 实际 | `currsize=500`，600 次请求全部为 misses（无命中），第一个视频已被淘汰（再次请求时 miss） |
| 结果 | ✅ 通过 |

---

### 测试 7：内存增长稳定性

| 项目 | 内容 |
|------|------|
| 目的 | 验证缓存在大量请求下不会导致内存泄漏 |
| 方法 | 连续请求 1000 个不同视频，每 100 次记录内存和缓存大小 |
| 预期 | 缓存大小稳定在 500，内存增长 < 50MB |

**内存增长记录**：

| 请求数 | 内存 (MB) | 缓存大小 |
|--------|-----------|----------|
| 100 | 63.93 | 100 |
| 200 | 64.04 | 200 |
| 300 | 64.14 | 300 |
| 400 | 64.27 | 400 |
| 500 | 64.39 | 500 |
| 600 | 64.49 | 500 |
| 700 | 64.59 | 500 |
| 800 | 64.69 | 500 |
| 900 | 64.79 | 500 |
| 1000 | 64.91 | 500 |

| 指标 | 数值 |
|------|------|
| 初始内存 | 63.82 MB |
| 最终内存 | 64.91 MB |
| 内存增长 | **1.09 MB**（远低于 50MB 阈值） |
| 缓存大小 | 稳定在 500（达到上限后不再增长） |

| 结果 | ✅ 通过 |
|------|------|

---

### 测试 8：返回结构一致性

| 项目 | 内容 |
|------|------|
| 目的 | 验证缓存命中时返回结构与首次请求完全一致 |
| 方法 | 相同参数调用 5 次，检查所有字段是否存在且值一致 |
| 预期 | 5 次返回完全相同，包含所有必需字段 |
| 实际 | 5 次返回完全一致，包含 `duration, width, height, codec, format, size` 全部字段 |
| 结果 | ✅ 通过 |

---

## 三、测试总结

### 总体结果

| 类别 | 通过 | 失败 | 总计 |
|------|------|------|------|
| 功能测试（缓存命中/未命中/失效/隔离） | 4 | 0 | 4 |
| 性能测试 | 1 | 0 | 1 |
| 容量测试（大小限制 + LRU 淘汰） | 1 | 0 | 1 |
| 稳定性测试（内存泄漏） | 1 | 0 | 1 |
| 兼容性测试（返回结构一致性） | 1 | 0 | 1 |
| **总计** | **8** | **0** | **8** |

### 关键指标达成情况

| 指标 | 目标 | 实际 | 状态 |
|------|------|------|------|
| 缓存命中耗时 | < 10ms | 0.003ms | ✅ 远超目标 |
| 性能提升 | > 90% | 99.97%（4412x） | ✅ 远超目标 |
| 缓存大小限制 | <= 500 | 500 | ✅ 精确符合 |
| 内存增长 | < 50MB | 1.09MB | ✅ 远低于阈值 |
| 返回结构一致 | 与修改前一致 | 完全一致 | ✅ 符合 |

### 结论

`_cached_video_metadata` 缓存修复实现正确，所有测试场景均通过验证：

1. **缓存机制正确**：`lru_cache` 基于 `(file_path, mtime)` 作为 key，命中/未命中行为符合预期
2. **缓存失效可靠**：文件修改（mtime 变化）后缓存自动失效，返回最新数据
3. **缓存隔离有效**：不同视频文件的缓存互不干扰
4. **性能提升显著**：缓存命中耗时从 ~11ms 降至 0.003ms，提升 4412 倍
5. **内存安全可控**：缓存大小严格限制在 500 条，1000 次请求仅增长 1.09MB，无泄漏风险
6. **返回结构稳定**：缓存命中与未命中返回的数据结构完全一致，前端无需任何修改

**Go/No-Go 建议**：✅ **GO** — 可以进入下一阶段。
