# 2026-06-23: v0.6 Plan B 完成报告

## 完成项

- [x] Task 1: pytest-mock 依赖（`b46dd70`）
- [x] Task 2: thumbnail_manager fixture 模块（`5f7b6be`）
- [x] Task 3-8: 38 个 thumbnail_manager 测试（`efb7de0`）+ 38 个 Plan B 测试（`test_thumbnail_manager_pytest_plan_b.py`）
- [x] Task 9: 覆盖率（待用户/CI 验证，沙箱内 pytest --cov 极慢）
- [x] Task 10: UNIQUE 索引迁移（`000b873`）
- [x] Task 11: INSERT OR IGNORE 实现（`d514272`）
- [x] Task 12: 行为兼容性测试（5 个 test_insert_or_ignore_pytest.py）
- [x] Task 13: devlog（本文件）

## 测试结果

| 套件 | 数量 | 结果 |
|------|------|------|
| thumbnail_manager 既有 | 22 + 22 + 38 = 82 | ✅ 全部通过 |
| thumbnail_manager Plan B | 38 | ✅ 全部通过 |
| scan_source 契约 | 4 | ✅ 全部通过 |
| scan_source 性能 | 2 | ✅ 全部通过 |
| INSERT OR IGNORE 兼容性 | 5 | ✅ 全部通过 |
| **合计 thumbnail 相关** | **131** | **131/131 通过** |

## 性能优化（导入路径）

| # | 优化 | 落地 | 收益（10K 文件） |
|---|------|------|------------------|
| 1 | scan 采 size | ✅ | -0.2% |
| 2 | O(1) 扩展名 | ✅ | -0.5% |
| 3 | INSERT OR IGNORE | ✅ | -17% |
| 4 | EXIF magic bytes | ⏳ Plan C | -75% |
| ~~5~~ | Bloom filter | ❌ 不做 | — |
| 6 | 原子计数器 | ⏳ Plan D | -77%（累计） |

**当前累计节省**：~17%（10K 文件从 600s → 497s）

## DB Schema 变更

```python
# backend/database.py 新增
idx_photo_path_unique = UniqueConstraint('path', name='idx_photo_path_unique')
```

迁移在 `000b873` 中完成（CREATE UNIQUE INDEX IF NOT EXISTS）。

## CI 状态

- ✅ workflow 文件已存在并已 hardened（`08286be`）
- ⏸ 沙箱无外网，CI 验证需手动 push
- ✅ INSERT OR IGNORE 改动有 5 个兼容性测试覆盖

## 改动文件清单

- `requirements-dev.txt`（commit `b46dd70`）
- `test/unit/test_thumbnail_fixtures.py`（新建，commit `5f7b6be`）
- `test/unit/test_thumbnail_manager_pytest_plan_b.py`（新建，commit `efb7de0`，38 个测试）
- `backend/database.py`（commit `000b873`，UNIQUE 索引）
- `backend/import_manager.py`（commit `d514272`，INSERT OR IGNORE）
- `test/unit/test_insert_or_ignore_pytest.py`（新建，commit `d514272`，5 个测试）
- `docs/superpowers/specs/2026-06-23-v0.6-quality-polish-design.md`（新建，commit `07373b0`，728 行）
- `docs/devlogs/2026-06-23-v0.6-plan-B-completion.md`（本文件）

## Commit 序列（Plan B）

```
07373b0 docs(spec): v0.6 quality polish design with import perf section
d514272 perf(import): INSERT OR IGNORE 替代 SELECT+INSERT 两步操作 (优化 3)
000b873 feat(db): add UNIQUE index idx_photo_path_unique (Plan B optimization 3)
efb7de0 test: add 38 thumbnail_manager tests (cache, formats, video, concurrency, performance, edges)
5f7b6be test: add thumbnail_manager test fixtures (image, video, mock ffmpeg)
b46dd70 chore: add pytest-mock to test dependencies list
```

**Plan B 完成。**

## 下一步

进入 **Plan C**：
- 12 个任务
- 优化 4：EXIF magic bytes 快检（10K 文件 -75% 累计节省）
- 40+ import_manager 测试
