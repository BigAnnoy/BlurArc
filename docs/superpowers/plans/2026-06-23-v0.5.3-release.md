# v0.5.3 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 4 个未推送 commit（移动端 UI 对齐 + 性能 + 修复）正式发布为 v0.5.3，CHANGELOG、Git tag、GitHub 三者对齐。

**Architecture:** 本次发布只动一个文件（`CHANGELOG.md`），通过 5 个独立可验证的 Git 操作完成任务。所有动作可回退。

**Tech Stack:** Git, PowerShell, `gh` CLI (optional)

**前置文档：** [docs/superpowers/specs/2026-06-23-v0.5.3-release-design.md](../specs/2026-06-23-v0.5.3-release-design.md)

---

## File Structure

**Files to modify:**
- `CHANGELOG.md` - 在 `[0.5.3] — 2026-06-21` 章节的「修复」「变更」末尾各追加内容

**Files NOT to modify:**
- ❌ `frontend/package.json` （已是 0.5.3）
- ❌ `src/BlurArc.py` 窗口标题（已是 v0.5.3）
- ❌ `backend/mobile_access_server.py` 的 `app_version: "1.0"`（mobile API 内部版本，独立体系）
- ❌ `dist/` （不重建，留给后续 CI/CD）

**Tests:**
- 不需要写新测试
- 8 条验收标准（AC-1 ~ AC-8）通过命令行验证

---

## Task 1: 编辑 CHANGELOG.md 追加「修复」内容

**Files:**
- Modify: `CHANGELOG.md` （在 v0.5.3 章节的「修复」末尾追加）

- [ ] **Step 1: 打开 CHANGELOG.md 定位 v0.5.3 章节的「修复」末尾**

```bash
# 找到 v0.5.3 章节「修复」末尾位置（双实例问题之前）
Get-Content CHANGELOG.md | Select-String -Pattern "并发阻塞|会话残留|## \[0.5.2\]" | Select-Object -First 5
```

预期输出：3 行命中（"并发阻塞"、"会话残留"、"## [0.5.2]"），"会话残留"那行就是「修复」小节的最后一条。

- [ ] **Step 2: 在「会话残留」行之前插入 8 条新修复项**

在 `CHANGELOG.md` 中找到 `- **会话残留**：`那一行，在它**上方**插入以下内容（注意是插入到 `### 修复` 区域里，不是 `### 变更`）：

```markdown
- **移动端 UI 与原型对齐**（10 项修复）：暗色 AppBar/BottomNav 背景色、AppBar logo 尺寸、工具栏下拉箭头、上传按钮布局、Settings 主题选项顺序、Section header 平台差异化、Settings 区块标题（仅 mobile）、Folder 返回按钮、BlurArcLogo 默认色对比度优化
- **平板 UI 与原型对齐**（10 项修复）
- **平板模拟器 UI 修复**：导航栏/工具栏对齐
- **mDNS 广播修复**：`ServiceInfo` 参数顺序（`port` 必须在 `addresses` 之前）、自动启动、`wait_ready()` 确认
- **导入预检性能修复**：删除 `rglob` 兜底，目标目录去重改用集合，导入时只对大小相同的组计算 MD5
- **手机首屏秒加载**：`Dio` 添加 `sendTimeout` 防卡死、SQL 添加复合索引
- **移动端 6 Bug 修复**：Logo 资源使用错误、文件夹返回按钮文本（"返回相册"→"返回上级"）
- **代码审查 4 项修复**：
  - `home_page.dart` 断连页溢出保护（`SafeArea + SingleChildScrollView`）
  - `api_server.py` 缓存命中时复用 size，去掉 `stat()` I/O
  - `api_client.dart` `onDisconnected` 触发条件收敛（新增 `_everConnected` 标志）
  - `dev-start.bat` 抽取 `:deploy_avd_common` 公共函数

```

- [ ] **Step 3: 验证 v0.5.3 章节的「修复」已扩展**

```bash
sed -n '/^## \[0.5.3\]/,/^## \[0.5.2\]/p' CHANGELOG.md
```

预期：能看到 12 条「修复」项（原 4 条 + 新增 8 条），"会话残留" 仍是「修复」区域最后一条，"### 变更" 紧接其后。

---

## Task 2: 编辑 CHANGELOG.md 追加「变更」内容

**Files:**
- Modify: `CHANGELOG.md` （在 v0.5.3 章节的「变更」末尾追加）

- [ ] **Step 1: 定位 v0.5.3 章节的「变更」末尾**

`### 变更` 区域当前 2 条：
- 版本号统一为 v0.5.3
- `dev-start.bat` / `dev-start.ps1` 移入 `scripts/` 目录，新增手机部署选项

`## [0.5.2] — 2026-06-18` 是 v0.5.3 章节的结束。

- [ ] **Step 2: 在「`dev-start.bat` 移入 `scripts/` 目录」之后、`## [0.5.2]` 之前插入 3 组新变更项**

```markdown

**功能改进：**
- **设备信息服务**：新增 `DeviceInfoService` 缓存设备名称，避免重复 IPC
- **Android 高刷屏支持**：`MainActivity.kt` 添加 `Window.setFrameRate` API，90/120Hz
- **iOS 应用名称**：`AppDelegate.swift` 配置显示名 "Blur Arc"
- **导入去重 MD5 复用 DB 缓存**：`prescan_index` 改为 `(file, md5_hash, size)` 三元组，命中 DB 缓存时跳过 `stat()` I/O
- **手机端首屏秒加载**：照片列表分页 + 缩略图懒加载 + SQL 索引
- **导入预检并行化**：保持现有 `ThreadPoolExecutor` 并行 MD5 计算，新增"按文件大小分组后跳过不可能重复"的预筛

**UI 调整：**
- **Logo 资源统一管理**：从 SVG（`flutter_svg` 兼容性问题）改为 PNG 内置资源，新增 `title_bar_light.png` / `title_bar_dark.png`
- **新启动器图标**：5 个 mipmap 分辨率全部替换为新设计
- **暗色 AppBar 背景调整**：`darkBgCard` → `darkBgPage`（`#0c1117`），AppBar/BottomNav/平板工具栏统一透明
- **Section header 平台差异化**：mobile `14×12×8 + 13px`、tablet `16×16×8 + 14px`
- **Settings 主题选项顺序**：跟随系统 / 深色 / 浅色（原：跟随系统 / 浅色 / 深色）
- **手机上传按钮布局**：Column 上下堆叠（"开始上传"在上，"全部取消"在下）

**开发工具：**
- **`dev-start` 热更新选项**：
  - `[9]` Flutter run + hot reload（手机端）
  - `[10]` Flutter run + hot reload（平板模拟器）
  - `[5]` PC 端自动启动（前端构建 + `BlurArc.py`）
- **清理临时脚本** `check_logo.py`（16 行，已无人引用）
- **部署脚本去重**：`dev-start.bat` / `dev-start.ps1` 部署去重 + 抽取 `:deploy_avd_common`
```

- [ ] **Step 3: 完整审视 v0.5.3 章节**

```bash
# 用 Git diff 看本次修改的总览
git diff CHANGELOG.md | head -100
```

预期：本次 diff 只增加新条目，不修改原有 v0.5.2 / v0.5.3 任何原文。"## [0.5.2]" 之前的所有新内容都已就位。

---

## Task 3: 提交 CHANGELOG 改动

**Files:**
- Modify: `CHANGELOG.md`（仅本文件）

- [ ] **Step 1: 确认 working tree 只有 CHANGELOG.md 修改**

```bash
git status
```

预期输出：
```
Changes to be committed:
  modified:   CHANGELOG.md
```

（不应有其他文件）

- [ ] **Step 2: 提交**

```bash
git add CHANGELOG.md
git commit -m "docs: 补充 v0.5.3 变更说明（移动端 UI + 性能 + 修复）"
```

预期输出：
```
[main <new-sha>] docs: 补充 v0.5.3 变更说明（移动端 UI + 性能 + 修复）
 1 file changed, 35 insertions(+)
```

- [ ] **Step 3: 验证 commit 数符合预期**

```bash
# 应显示领先 origin/main 5 个 commit
git log origin/main..HEAD --oneline
```

预期：5 行 commit（4 个已有 + 1 个新 CHANGELOG commit）

---

## Task 4: 推送到 origin/main

**Files:** （无文件改动）

- [ ] **Step 1: 推前最后一次安全检查**

```bash
git status
```

预期：`Your branch is ahead of 'origin/main' by 5 commits.`

- [ ] **Step 2: 推送**

```bash
git push origin main
```

预期输出：
```
To https://github.com/BigAnnoy/BlurArc
   <old-sha>..<new-sha>  main -> main
```

- [ ] **Step 3: 验证推送成功**

```bash
git fetch
git status
```

预期：`Your branch is up to date with 'origin/main'.`

---

## Task 5: 创建 v0.5.3 附注型 tag

**Files:** （无文件改动）

- [ ] **Step 1: 确认 v0.5.3 tag 还不存在**

```bash
git tag --list v0.5.3
```

预期：空输出（tag 还不存在）

- [ ] **Step 2: 创建附注型 tag**

```bash
git tag -a v0.5.3 -m "Release v0.5.3 — 移动端 UI 优化 + 性能修复 + Bug 修复

主要变更：
- 移动端 UI 与原型对齐（10 项修复）
- 平板 UI 与原型对齐（10 项修复）
- mDNS 广播修复（自动发现可用）
- 导入预检性能优化（去重 + 缓存复用）
- 手机首屏秒加载（SQL 索引 + 分页）
- 代码审查 4 项修复
- Logo 资源统一 + 设备信息服务
- dev-start 热更新选项（[9]/[10]/[5]）
- Android 高刷屏支持（90/120Hz）
- 删除临时脚本 check_logo.py

完整变更见 CHANGELOG.md"
```

预期输出：无（创建成功）。如果出现 "fatal: tag already exists" 错误，回到 Step 1 排查。

- [ ] **Step 3: 验证 tag 已创建并查看内容**

```bash
git tag -l --format='%(contents)' v0.5.3
```

预期：完整的多行 release notes（与上方 `-m` 内容一致）

---

## Task 6: 推送 v0.5.3 tag 到 origin

**Files:** （无文件改动）

- [ ] **Step 1: 推送 tag**

```bash
git push origin v0.5.3
```

预期输出：
```
To https://github.com/BigAnnoy/BlurArc
 * [new tag]         v0.5.3 -> v0.5.3
```

- [ ] **Step 2: 远端验证 tag**

```bash
git ls-remote --tags origin | Select-String "v0.5.3"
```

预期输出：包含 `refs/tags/v0.5.3` 的一行

---

## Task 7: 验收标准 8 条全部通过

**Files:** （无文件改动）

- [ ] **AC-1: CHANGELOG.md v0.5.3 章节「修复」「变更」子分类已扩展**

```bash
sed -n '/^## \[0.5.3\]/,/^## \[0.5.2\]/p' CHANGELOG.md
```

预期：看到 12 条「修复」 + 17 条「变更」（原 2 条 + 新增 15 条），且「修复」「变更」之外**没有**新子分类。

- [ ] **AC-2: origin/main 与本地 main 同步**

```bash
git fetch
git status
```

预期：`Your branch is up to date with 'origin/main'.`

- [ ] **AC-3: GitHub 上可见 v0.5.3 附注型 tag**

```bash
git ls-remote --tags origin | Select-String "v0.5.3"
```

预期：输出一行 `refs/tags/v0.5.3`

- [ ] **AC-4: tag 消息包含完整 release notes**

```bash
git tag -l --format='%(contents)' v0.5.3 | Measure-Object -Line
```

预期：Lines >= 12（即 tag 消息至少 12 行）

- [ ] **AC-5: 工作区干净**

```bash
git status
```

预期：`nothing to commit, working tree clean`

- [ ] **AC-6: mobile_access_server.py 的 app_version: "1.0" 未被修改**

```bash
git show HEAD:backend/mobile_access_server.py | Select-String "app_version"
```

预期：唯一一行 `app_version: "1.0"`（如果出现 v0.5.4 之类，说明误改了）

- [ ] **AC-7: frontend/package.json 仍是 0.5.3**

```bash
(Get-Content frontend/package.json -Raw | ConvertFrom-Json).version
```

预期：`0.5.3`

- [ ] **AC-8: src/BlurArc.py 窗口标题仍是 v0.5.3**

```bash
git grep "title='Blur Arc"
```

预期：唯一一行 `src/BlurArc.py:title='Blur Arc v0.5.3'`

---

## Task 8: （可选）创建 GitHub Release

**Files:** （无文件改动）

- [ ] **Step 1: 检查 gh CLI 是否已认证**

```bash
gh auth status
```

如果显示 "You are not logged into any GitHub hosts. Please run 'gh auth login' to authenticate."，则**跳过整个 Task 8**，告诉用户手动在 GitHub 网页创建 Release。

- [ ] **Step 2: 截取 CHANGELOG.md 的 v0.5.3 章节到临时文件**

```powershell
# PowerShell 等价命令
$content = Get-Content CHANGELOG.md -Raw
$start = $content.IndexOf("## [0.5.3]")
$end = $content.IndexOf("## [0.5.2]")
$section = $content.Substring($start, $end - $start).TrimEnd()
$section | Out-File -FilePath .release-notes-0.5.3.md -Encoding utf8
Get-Content .release-notes-0.5.3.md
```

预期：终端输出 v0.5.3 整段内容到 `.release-notes-0.5.3.md`

- [ ] **Step 3: 创建 GitHub Release**

```bash
gh release create v0.5.3 --title "Blur Arc v0.5.3" --notes-file .release-notes-0.5.3.md
```

预期输出：
```
https://github.com/BigAnnoy/BlurArc/releases/tag/v0.5.3
```

- [ ] **Step 4: 清理临时文件**

```bash
Remove-Item .release-notes-0.5.3.md
git status
```

预期：工作区 clean

---

## Task 9: 完成后给用户的总结

**Files:** （无文件改动）

- [ ] **Step 1: 给出 v0.5.3 发布完成报告**

向用户报告：
1. 推送了多少个 commit
2. tag v0.5.3 的 SHA 与内容预览
3. GitHub Release URL（如果 Step 8 成功）
4. 下一阶段建议（CI/CD、相似照片识别等）

---

## 回退方案（如需）

| 场景 | 命令 |
|------|------|
| 撤回 commit 推送 | `git push --force-with-lease origin main`（仅在远端未被人新推时） |
| 撤回 tag | `git tag -d v0.5.3 ; git push origin :refs/tags/v0.5.3` |
| 撤回单个 CHANGELOG commit | `git revert <changelog-commit-sha>` |
| 完全回退到 origin/main | `git reset --hard origin/main`（不可逆，丢弃本地所有） |
