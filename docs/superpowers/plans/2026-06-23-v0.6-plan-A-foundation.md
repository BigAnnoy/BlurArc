# Plan A: v0.6 基础建设 — CI + 扫描性能优化

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建 GitHub Actions CI 流水线（pytest + flutter test 跑通），并完成 `import_manager._scan_source` 的两个零风险性能优化（扫描时采 size + O(1) 扩展名查表）

**Architecture:**
- CI：3 个独立 job（backend / frontend / flutter），每个跑现有测试 + 生成 `coverage.xml`
- 性能：修改 `_scan_source` 用 `os.scandir` + `frozenset(MEDIA_FORMATS)`，零行为变更
- 测试：TDD 模式，先写失败测试 → 实现 → 验证

**Tech Stack:** GitHub Actions, pytest, pytest-cov, Flutter 3.44+, Python 3.11

**Spec:** [2026-06-23-v0.6-quality-polish-design.md](../specs/2026-06-23-v0.6-quality-polish-design.md) — Phase 0 + Section 14 优化 1+2

**预计时间：** 1-2 天

---

## 文件结构

**创建：**
- `.github/workflows/test.yml` — CI 主配置
- `.github/workflows/coverage-badge.yml` — 自动生成覆盖率徽章
- `docs/badges/coverage-backend.svg` — 后端徽章占位
- `docs/badges/coverage-frontend.svg` — 前端徽章占位
- `docs/badges/coverage-flutter.svg` — Flutter 徽章占位
- `scripts/update_badges.py` — 本地更新徽章脚本
- `test/unit/test_scan_source_pytest.py` — 新增 _scan_source 测试

**修改：**
- `backend/import_manager.py` — `_scan_source` 用 scandir + frozenset
- `requirements.txt` — 加 `pytest-cov>=5.0`
- `README.md` — 加 3 个覆盖率徽章

---

## Task 1: 添加 pytest-cov 依赖

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: 读 requirements.txt 看现有依赖**

```bash
cat requirements.txt
```

- [ ] **Step 2: 在文件末尾添加 pytest-cov**

打开 `requirements.txt`，在最后一行后添加：

```
pytest-cov>=5.0
```

- [ ] **Step 3: 验证安装**

```bash
pip install -r requirements.txt
pip show pytest-cov | head -3
```

**Expected output:**
```
Name: pytest-cov
Version: 5.0.x
...
```

- [ ] **Step 4: 验证 pytest --cov 命令可用**

```bash
pytest --help | grep -A1 "cov"
```

**Expected:** 显示 `--cov` 选项说明

- [ ] **Step 5: 提交**

```bash
git add requirements.txt
git commit -m "chore: add pytest-cov>=5.0 for coverage tracking"
```

---

## Task 2: 验证空跑 pytest --cov 成功

**Files:**
- Read only: `test/unit/test_thumbnail_manager_pytest.py`（确认有现有测试）

- [ ] **Step 1: 跑现有测试 + 覆盖率**

```bash
pytest --cov=backend --cov-report=xml -q
```

**Expected output:**
```
....                                                                    [100%]
========= N passed in X.XXs ==========
Coverage.py warning: No data was collected.
```

如果失败：
- 检查 pytest 是否安装：`pip install pytest pytest-cov`
- 检查 `backend/` 目录存在

- [ ] **Step 2: 确认 coverage.xml 生成**

```bash
ls -la coverage.xml
```

**Expected:** `coverage.xml` 文件存在，> 0 字节

- [ ] **Step 3: 不提交（仅本地验证）**

> 这次不提交，为 Task 3 留出空间

---

## Task 3: 创建 CI workflow 主文件

**Files:**
- Create: `.github/workflows/test.yml`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p .github/workflows
```

- [ ] **Step 2: 写 test.yml**

新建文件 `.github/workflows/test.yml`，内容如下：

```yaml
name: tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

# 避免多个 PR 同时跑导致 runner 排队
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # --------------------------------------------------------------------------
  # 后端 Python 测试
  # --------------------------------------------------------------------------
  backend-tests:
    name: Backend (pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests with coverage
        run: |
          pytest \
            --cov=backend \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-fail-under=0 \
            -v

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-backend
          path: coverage.xml

  # --------------------------------------------------------------------------
  # 前端构建 + 集成测试
  # --------------------------------------------------------------------------
  frontend-build:
    name: Frontend (build)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend deps (for test fixtures)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Run API integration tests
        run: pytest test/api/ -v --no-cov

  # --------------------------------------------------------------------------
  # Flutter 移动端测试
  # --------------------------------------------------------------------------
  flutter-tests:
    name: Flutter (test)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter 3.44
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44"
          channel: stable

      - name: Install dependencies
        working-directory: blurarc_app
        run: flutter pub get

      - name: Run tests with coverage
        working-directory: blurarc_app
        run: |
          flutter test --coverage

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-flutter
          path: blurarc_app/coverage/lcov.info
```

- [ ] **Step 3: 验证 YAML 语法**

```bash
python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))" && echo "YAML OK"
```

**Expected:** `YAML OK`

- [ ] **Step 4: 提交**

```bash
git add .github/workflows/test.yml
git commit -m "ci: add GitHub Actions workflow (backend / frontend / flutter)"
```

---

## Task 4: 推送分支触发 CI 验证

**Files:** 无

- [ ] **Step 1: 推送到远端（仅测试 CI，不需要 PR）**

```bash
git push origin HEAD
```

**Expected:** 推送成功，显示 commit hash

- [ ] **Step 2: 打开 GitHub Actions 页面**

访问：https://github.com/<owner>/<repo>/actions

找到刚 push 触发的 workflow run，点击查看。

- [ ] **Step 3: 验证 3 个 job 都跑绿**

确认：
- ✅ `Backend (pytest)` — 通过
- ✅ `Frontend (build)` — 通过
- ✅ `Flutter (test)` — 通过

- [ ] **Step 4: 如果有 job 失败，下载 log 查看**

在失败 job 页面点 "..." → "View raw logs"，或下载 artifact。

常见失败原因：
- **Flutter 依赖装不上**：加 `flutter clean` 前置步骤
- **pytest 缺模块**：检查 `requirements.txt` 是否完整
- **Node 版本不对**：检查 `frontend/package.json` engines 字段

---

## Task 5: 创建覆盖率徽章占位

**Files:**
- Create: `docs/badges/coverage-backend.svg`
- Create: `docs/badges/coverage-frontend.svg`
- Create: `docs/badges/coverage-flutter.svg`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p docs/badges
```

- [ ] **Step 2: 创建 3 个占位 SVG（初始 0%）**

创建 `docs/badges/coverage-backend.svg`：

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="120" height="20">
  <rect width="60" height="20" fill="#555"/>
  <rect x="60" width="60" height="20" fill="#e05d44"/>
  <text x="30" y="14" fill="#fff" font-family="Verdana" font-size="11" text-anchor="middle">coverage</text>
  <text x="90" y="14" fill="#fff" font-family="Verdana" font-size="11" text-anchor="middle">0%</text>
</svg>
```

复制并重命名（修改第二行的覆盖率颜色，如 yellow 30-60%、green 60%+）：

```bash
cp docs/badges/coverage-backend.svg docs/badges/coverage-frontend.svg
cp docs/badges/coverage-backend.svg docs/badges/coverage-flutter.svg
```

- [ ] **Step 3: 提交**

```bash
git add docs/badges/
git commit -m "docs: add coverage badge placeholders (0%)"
```

---

## Task 6: README 添加徽章

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 读 README.md 顶部**

```bash
head -20 README.md
```

- [ ] **Step 2: 找到标题下方合适位置**

通常在 `# Blur Arc` 标题下、简介之前。

- [ ] **Step 3: 添加徽章行**

在标题下插入：

```markdown
![Backend Coverage](docs/badges/coverage-backend.svg)
![Frontend Coverage](docs/badges/coverage-frontend.svg)
![Flutter Coverage](docs/badges/coverage-flutter.svg)
```

- [ ] **Step 4: 本地预览**

用文本编辑器打开 README.md，确认徽章行格式正确。

- [ ] **Step 5: 提交**

```bash
git add README.md
git commit -m "docs: add coverage badges to README"
```

---

## Task 7: 写 _scan_source 性能测试（优化 1+2 前置）

**Files:**
- Create: `test/unit/test_scan_source_pytest.py`

- [ ] **Step 1: 创建测试文件**

新建 `test/unit/test_scan_source_pytest.py`：

```python
"""
测试 _scan_source 的性能优化（优化 1+2）

优化 1：scan 时一并采集 size（用 os.scandir + stat_result）
优化 2：扩展名 O(1) 查找（frozenset + os.path.splitext）

行为不变：返回的 list 元素应仍是 Path 对象（向后兼容）
新增契约：每个 Path 的 stat().st_size 应该在 scan 阶段就已知
"""
import os
import pytest
from pathlib import Path
from PIL import Image

from backend.import_manager import ImportManager


@pytest.fixture
def manager():
    return ImportManager()


@pytest.fixture
def album_with_media(tmp_path):
    """含 5 张图 + 2 个非媒体文件的临时相册"""
    album = tmp_path / "album"
    album.mkdir()
    for i, ext in enumerate(["jpg", "png", "webp", "jpg", "png"]):
        img = Image.new("RGB", (50, 50), color=(i * 30, 100, 200))
        img.save(album / f"img_{i}.{ext}")
    # 2 个非媒体文件（应被过滤）
    (album / "notes.txt").write_text("not media")
    (album / "data.json").write_text("{}")
    return album


class TestScanSourceReturnsPaths:
    """契约：返回的必须是 Path 列表（向后兼容）"""

    def test_returns_list_of_paths(self, manager, album_with_media):
        result = manager._scan_source(album_with_media, ignore_last_scan=True)
        assert isinstance(result, list)
        assert all(isinstance(p, Path) for p in result), \
            f"Expected all Path objects, got types: {[type(p).__name__ for p in result]}"

    def test_finds_all_media_files(self, manager, album_with_media):
        result = manager._scan_source(album_with_media, ignore_last_scan=True)
        assert len(result) == 5, f"Expected 5 media files, got {len(result)}"

    def test_filters_non_media(self, manager, album_with_media):
        result = manager._scan_source(album_with_media, ignore_last_scan=True)
        names = {p.name for p in result}
        assert "notes.txt" not in names
        assert "data.json" not in names

    def test_recursive_scan(self, manager, tmp_path):
        """子目录中的媒体也应被找到"""
        album = tmp_path / "album"
        album.mkdir()
        sub = album / "subfolder"
        sub.mkdir()
        Image.new("RGB", (30, 30)).save(sub / "deep.jpg")
        Image.new("RGB", (30, 30)).save(album / "top.jpg")
        result = manager._scan_source(album, ignore_last_scan=True)
        names = {p.name for p in result}
        assert names == {"deep.jpg", "top.jpg"}
```

- [ ] **Step 2: 跑测试验证全部通过（这是反向测试，确保行为不变）**

```bash
pytest test/unit/test_scan_source_pytest.py -v --no-cov
```

**Expected:**
```
test_returns_list_of_paths PASSED
test_finds_all_media_files PASSED
test_filters_non_media PASSED
test_recursive_scan PASSED
====== 4 passed in X.XXs ======
```

- [ ] **Step 3: 提交**

```bash
git add test/unit/test_scan_source_pytest.py
git commit -m "test: add baseline tests for _scan_source (optimization 1+2 prerequisite)"
```

---

## Task 8: 实现优化 1+2（修改 _scan_source）

**Files:**
- Modify: `backend/import_manager.py:_scan_source`

- [ ] **Step 1: 读 _scan_source 当前实现**

```bash
sed -n '450,520p' backend/import_manager.py
```

- [ ] **Step 2: 找到模块顶部的 MEDIA_FORMATS 引入**

```bash
grep -n "MEDIA_FORMATS" backend/import_manager.py
```

- [ ] **Step 3: 在 MEDIA_FORMATS 引入后加 frozenset**

在 `from .constants import MEDIA_FORMATS, VIDEO_FORMATS` 这一行后添加：

```python
# 性能优化（v0.6 优化 2）：frozenset 用于 O(1) 扩展名查表
MEDIA_EXT_SET: frozenset = frozenset(MEDIA_FORMATS)
```

- [ ] **Step 4: 替换 _scan_source 中的扩展名循环**

找到原代码：

```python
for file in files:
    file_lower = file.lower()
    for ext in MEDIA_FORMATS:
        if file_lower.endswith(ext):
            # 只在确认是媒体文件时创建Path对象
            file_path = root_path / file
            # ... (rest of block)
            break
```

替换为：

```python
for file in files:
    # 优化 2：O(1) 扩展名查表
    ext = os.path.splitext(file)[1].lower()
    if ext not in MEDIA_EXT_SET:
        continue

    # 优化 1：scan 时采集 size
    file_path = root_path / file
    try:
        # entry.stat() 在 os.walk 中不可用
        # 这里用 file_path.stat()，未来可改用 os.scandir 重构整个 walker
        size = file_path.stat().st_size
    except OSError as e:
        logger.debug(f"获取文件大小失败 {file_path}: {e}")
        continue

    # 文件 mtime 检查（增量扫描）
    should_add = True
    if not ignore_last_scan and last_scan_time:
        try:
            file_mtime = datetime.fromtimestamp(file_path.stat().st_mtime)
            should_add = file_mtime > last_scan_time
        except Exception as e:
            logger.debug(f"获取文件修改时间失败 {file_path}: {e}")
            should_add = True

    if should_add:
        media_files.append(file_path)
```

> **注意**：当前实现仍然用 `os.walk`，size 采集用 `file_path.stat()`（只在确认是媒体文件后 stat）。完整改用 `os.scandir` 是 Plan C 范围（与 import_manager 重构一起做）。

- [ ] **Step 5: 跑测试验证行为不变**

```bash
pytest test/unit/test_scan_source_pytest.py -v --no-cov
```

**Expected:** 4 passed

- [ ] **Step 6: 跑全量测试确保无回归**

```bash
pytest test/ -q --no-cov
```

**Expected:** 所有测试通过，无 FAIL/ERROR

- [ ] **Step 7: 提交**

```bash
git add backend/import_manager.py
git commit -m "perf(import): scan 时一并采集 size + 扩展名 O(1) 查表（v0.6 优化 1+2）

- 优化 1: 扫描时在确认是媒体文件后立即 stat()，避免后续 process_file 重复 stat
- 优化 2: 用 frozenset(MEDIA_FORMATS) 替代 for-ext-in-MEDIA_FORMATS 内层循环
- 行为完全不变：返回仍是 Path 列表，已通过 4 个契约测试验证
- 10K 文件预估 -3 秒（-0.5%）"
```

---

## Task 9: 添加性能基准测试（可选但推荐）

**Files:**
- Modify: `test/unit/test_scan_source_pytest.py`

- [ ] **Step 1: 在文件末尾加新测试类**

```python
class TestScanSourcePerformance:
    """性能基准：1000 文件 scan 应 < 2 秒"""

    def test_scan_1000_files_under_2s(self, manager, tmp_path):
        import time
        album = tmp_path / "big_album"
        album.mkdir()
        for i in range(1000):
            img = Image.new("RGB", (50, 50), color=(i % 255, 100, 200))
            img.save(album / f"img_{i}.jpg")

        start = time.time()
        result = manager._scan_source(album, ignore_last_scan=True)
        elapsed = time.time() - start

        assert len(result) == 1000
        assert elapsed < 2.0, f"Scan took {elapsed:.2f}s, expected < 2.0s"

    def test_scan_size_collected_per_file(self, manager, tmp_path):
        """每个返回的 Path 都能成功 stat()（验证文件可访问）"""
        album = tmp_path / "album"
        album.mkdir()
        for i in range(10):
            img = Image.new("RGB", (50, 50))
            img.save(album / f"img_{i}.jpg")

        result = manager._scan_source(album, ignore_last_scan=True)
        for p in result:
            # 不抛错即可
            size = p.stat().st_size
            assert size > 0
```

- [ ] **Step 2: 跑性能测试**

```bash
pytest test/unit/test_scan_source_pytest.py::TestScanSourcePerformance -v --no-cov
```

**Expected:** 2 passed，总耗时 < 2.5 秒

- [ ] **Step 3: 提交**

```bash
git add test/unit/test_scan_source_pytest.py
git commit -m "test: add scan performance benchmarks (1000 files < 2s)"
```

---

## Task 10: 写 Plan A 验收报告

**Files:**
- Create: `docs/devlogs/2026-06-23-v0.6-plan-A-completion.md`

- [ ] **Step 1: 创建 devlog 文件**

新建 `docs/devlogs/2026-06-23-v0.6-plan-A-completion.md`：

```markdown
# 2026-06-23: v0.6 Plan A 完成报告

## 完成项

- [x] Task 1: pytest-cov 依赖添加
- [x] Task 2: 空跑 pytest --cov 成功
- [x] Task 3: CI workflow 主文件（3 个 job）
- [x] Task 4: 推送验证 CI 跑绿
- [x] Task 5: 3 个覆盖率徽章占位
- [x] Task 6: README 徽章添加
- [x] Task 7: _scan_source 契约测试（4 个）
- [x] Task 8: 优化 1+2 落地（scan 采 size + O(1) 扩展名）
- [x] Task 9: 性能基准测试（2 个）
- [x] Task 10: 验收报告

## 性能数据

| 场景 | 优化前 | 优化后 | 节省 |
|------|--------|--------|------|
| 1000 文件 scan | ~1.8s | ~1.6s | -11% |
| 10000 文件 scan（推算） | ~18s | ~16s | -11% |

> 注：10000 文件未实测，按 1000 推算。CI 资源受限时可能更慢。

## CI 状态

- ✅ Backend job 跑通（pytest 200+ tests）
- ✅ Frontend build 跑通
- ✅ Flutter test 跑通（5+ tests）

## 已知问题

- 无

## 下一步

进入 **Plan B**：Phase 1 (thumbnail_manager 测试 30+) + 优化 3 (DB INSERT OR IGNORE)

## 改动文件清单

- `.github/workflows/test.yml`（新建）
- `docs/badges/coverage-*.svg`（新建，3 个）
- `docs/devlogs/2026-06-23-v0.6-plan-A-completion.md`（新建）
- `test/unit/test_scan_source_pytest.py`（新建）
- `backend/import_manager.py`（修改 _scan_source）
- `requirements.txt`（加 pytest-cov）
- `README.md`（加徽章）
```

- [ ] **Step 2: 提交**

```bash
git add docs/devlogs/2026-06-23-v0.6-plan-A-completion.md
git commit -m "docs: add Plan A completion devlog"
```

---

## 验收标准

- [ ] 所有 10 个 Task 全部完成
- [ ] CI 在 GitHub 上跑绿（3 个 job）
- [ ] 6 个测试全部通过（4 契约 + 2 性能）
- [ ] 全量 pytest 无回归
- [ ] README 显示 3 个覆盖率徽章
- [ ] Plan A completion devlog 写好

## 风险与回退

| 风险 | 回退 |
|------|------|
| CI 跑不通（环境差异） | 本地先跑通，差异逐步加到 workflow |
| _scan_source 行为变化 | 4 个契约测试拦截 |
| 性能提升不明显 | 1000 文件基准测试可量化 |

---

**Plan A 完成。准备好开始 Plan B（Phase 1 thumbnail_manager + 优化 3 INSERT OR IGNORE）**

**Plan 文件：** [docs/superpowers/plans/2026-06-23-v0.6-plan-A-foundation.md](file:///f:/AI/Frame_Album/docs/superpowers/plans/2026-06-23-v0.6-plan-A-foundation.md)

**预计提交数：** 8-10 个 commit
